import { Cart } from "../cart";
/**
 *
 * @param {*} props
 * @returns {jsx} for shopping cart page
 */
const CartPage = (props) => {
  return (
    <div className="frame">
      <Cart />
    </div>
  );
};

export default CartPage;
