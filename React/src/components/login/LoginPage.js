import { Login } from "../login";

/**
 *
 * @param {*} props
 * @returns for login page
 */
const LoginPage = (props) => {
  return (
    <div className="frame primary-colour" style={{ margin: "0" }}>
      <Login />
    </div>
  );
};

export default LoginPage;
